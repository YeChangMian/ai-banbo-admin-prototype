# AI伴播管理后台 PRD（简化版 V3）

## 1. 文档信息

- 产品名称：AI伴播
- 版本：V3 菜单整合原型
- 更新时间：2026-09-16

## 2. 产品定位

AI伴播面向有固定直播频次的品牌商家、直播运营团队及直播代运营团队，当前聚焦服饰直播和 IP 形象直播两类场景。

核心用户包括直播运营、中控、内容制作人员和团队管理员。这些用户熟悉商品和直播业务，但通常不理解动作流、生成子任务、模型参数等 AI 生产概念。

产品当前覆盖两类核心使用场景：

1. 服饰展示：通过真人数字模特展示服饰商品、款色及商品搭配，补充真人主播无法持续、完整展示商品效果的问题。
2. IP 形象互动：通过动画或品牌 IP 形象响应主播口令、弹幕评论等直播间互动，增强直播内容表现和观众陪伴感。

系统通过可复用展示模版降低伴播视频制作门槛，并让生成结果能够在直播中按商品讲解、主播口令或弹幕评论自动触发。

## 3. 版本目标

V3 的目标是继续减少用户必须理解的菜单和素材管理入口，把“选素材”和“管理素材”合并到制作场景中，同时不减少业务判断所需的信息。

本版本必须区分：

- 功能简化：减少菜单层级、重复入口、重复配置和内部任务概念。
- 信息完整：列表、详情、结果确认和管理页面继续展示用户判断与操作所需的完整字段。

不得以“简化”为理由删除商品图片类型、SKU 款色、触发条件、创建信息、账号状态等有效业务字段。

普通用户只需要理解两个核心动作：制作伴播、管理伴播视频。

主要制作流程收敛为：

`选择展示模版 → 选择商品 → 选择数字模特 → 使用背景 → 制作并确认`

动作、动作流、生成任务、子任务和技术状态仍可作为系统内部能力存在，但不在普通用户主流程中直接展示。

## 4. 信息架构

### 4.1 工作台

展示当天最需要关注的信息：

- 正在直播：直播账号、当前视频封面、视频名称、伴播时长、播放次数。
- 数据统计支持日、周、月和自定义时间。
- 伴播数据：伴播时长、触发次数、伴播商品数、播放视频数和趋势图。
- 生成数据：生成视频数量、生成时长、消耗金额、重新生成率和趋势图。
- 待处理：待确认视频、需要重新生成的视频、新同步商品。

工作台不展示商品、项目结构或系统能力说明，避免重复信息。

### 4.2 一级菜单

左侧导航只展示四个一级菜单：

- 工作台
- 制作伴播
- 伴播视频库
- 设置

商品列表、数字模特和展示模版不再作为独立菜单出现，其内容与管理操作整合到制作伴播流程中。历史制作任务通过制作伴播右侧入口进入，不占用一级菜单。

## 5. 核心功能

### 5.1 制作伴播

#### 步骤一：选择展示模版

- 用户先选择本次伴播视频的展示方式，再进入商品、数字模特和使用背景配置；未选择展示模版时不能进入下一步。
- 展示模版分为“服饰展示”和“IP形象互动”两类，卡片明确展示模版类型、封面、名称和完整触发方式。
- 当前步骤支持“创建展示模版”和查看“详情”；模版详情展示模版类型、参考素材、提示词、生成尺寸、生成时长和触发方式。
- 选择“服饰展示”模版后，第三步仅允许选择真人数字模特，IP 动画形象置灰且不可选。
- 选择“IP形象互动”模版后，第三步仅允许选择 IP 动画形象，真人数字模特置灰且不可选。
- 如果用户切换模版后，当前已选数字模特与新模版类型不兼容，系统自动清空数字模特选择。

#### 步骤二：商品选择与搭配

- 商品区标题为“商品选择与搭配（选填）”。标题区域右侧以店铺图标、店铺名称和切换操作展示当前店铺，并与商品搜索框同一行；排序在下一行靠右展示，添加商品图标紧跟在排序右侧，图标悬浮说明完整名称。
- 制作页不默认展示店铺全部商品，只展示已导入并完成分析的伴播商品库商品，避免几千至上万件商品首次进入页面时产生不必要的商品分析消耗。
- 点击“添加商品”打开店铺商品弹窗，左侧提供店铺商品类目筛选，右侧商品以 4 列豆腐块展示，桌面端默认 2 行 4 列。每个 SPU 商品块展示商品图、可选 SKU、商品名称、商品 ID 和货号，其中 SKU 选择区位于商品标题上方。商品详情分析尚未完成前，商品卡不展示类目、季节或其他商品标签；左侧类目仅使用店铺同步的基础类目数据进行筛选。
- 添加商品弹窗尺寸固定，底部操作栏固定，中间商品区域独立滚动；默认按“近7天销量 Top”展示，也可切换为“最新上架”。用户选择商品后点击“添加并分析”，商品进入伴播商品库并开始商品信息分析。
- 商品信息分析需要复用近 30 日内的分析结果：如商品在最近 30 日已经完成过商品信息分析，则本次添加直接复用已有结果，不重复调用分析资源；超过 30 日或没有分析记录时才重新分析。
- 支持按商品名称或商品 ID 搜索。
- 默认按近 7 天销量倒序，可通过“近7天销量排序｜上架时间排序”文字按钮切换排序。
- 已导入商品支持按“近7天销量排序｜上架时间排序”切换排序。
- 商品为选填项，支持多选；不选择商品时，可继续制作不关联商品的伴播视频。
- 商品按 SKU 维度加入制作页。同一 SPU 一次选择两个或更多款色 SKU 时，添加完成后各款色仍分别作为未分组 SKU 展示，不默认组成搭配组；同一 SPU 或不同 SPU 的 SKU 均需由用户主动拖动叠加组成搭配组，也可拖入已有搭配组。
- 商品链接详情分析需识别实际存在的商品视角，并保存为商品视角数据。视角包括正、反、侧、上、底、背等；只展示解析到的视角，不为缺失的图片补齐标签。未加入搭配组的 SKU 卡片在商品图左上角展示视角方块；加入搭配组后，搭配组 3×3 商品缩略图不展示视角标签；打开搭配组明细弹窗后，在类目、款色和货号信息下方以独立方块展示对应视角，不覆盖商品图片。
- 商品步骤中，未手动组成搭配组的单个 SKU 仍以独立商品展示，不提前显示为搭配组；点击“下一步”进入第三步时，系统才将每个未分组 SKU 自动视为一个独立搭配组。
- 搭配组卡片仅展示 SKU 商品图，并将搭配组名称和商品数量统一放在图片区域下方，不在卡片中重复展示款色名称。搭配组图片区域固定为 3×3 共 9 个 1:1 图片位，商品不足 9 个时保留纯白空图片位；单品图片使用 1:1 正方形。两类卡片使用相同的信息区高度、整体高度和叉号位置，在同一网格中连续排列，桌面端每行展示 5 个。
- 搭配组卡片内的每张 SKU 图片均可直接拖动：拖到其他搭配组时移动到目标组，拖到未分组商品上时重新组成搭配组，拖到商品区域空白位置时移出原搭配组并恢复为未分组商品。用户不必进入搭配组明细弹窗即可完成移入、移出和重新搭配。
- 搭配组内剩余 1 件商品时仍保留该搭配组、搭配组名称和“1 件”数量；只有组内商品数量变为 0 时才自动删除搭配组。
- 商品选择与搭配页面中的商品均已加入本次制作，不再提供卡片二次选择状态；单品卡片右上角不展示选择圆圈，用户通过拖拽完成搭配，通过右上角叉号移除商品。
- 商品搭配区域及搭配组明细弹窗中的操作均使用图标按钮：“移出搭配组”使用退出箭头图标，“移除商品”使用叉号图标；商品卡片和搭配组卡片的叉号使用完全一致的尺寸与坐标，按钮中心点对齐卡片右上角，固定展示在卡片右上角外侧，并使用浅灰色按钮底保证清晰度。鼠标移入图标时通过提示文本说明操作含义。
- 搭配组卡片中，搭配组名称左对齐展示，商品数量靠右对齐展示。
- 未分组的单款 SKU 卡片辅助信息按“类目｜款色｜货号”展示，明确款色所属的商品类目。
- 未分组的单款 SKU 卡片中，商品名称只展示一行；名称超过卡片宽度时以省略号截断，鼠标移入可查看完整商品名称。
- 点击搭配组卡片打开搭配组明细，名称输入框下方需以“搭配组商品”作为商品列表提示；每条商品辅助信息按“类目｜款色｜货号”展示。弹窗继续支持修改搭配组名称和管理组内商品，作为查看与精细管理入口。
- 搭配组明细弹窗与页面全局点击事件隔离：名称输入框点击、聚焦和输入时弹窗保持打开；点击弹窗遮罩也不关闭，只允许通过右上角关闭、底部取消或保存操作关闭。
- 商品选择与搭配页提供“移除商品”操作：未分组 SKU 可直接移除，搭配组可整组移除，搭配组明细中也可单独移除某个 SKU。移除后商品不再出现在当前制作页，页面不提供撤销或直接恢复，只能通过“添加商品”重新添加。
- 单品卡片和搭配组卡片右上角均同时提供“复制”和“移除”图标。复制单品会创建一个外观、商品信息和 SKU 均相同的独立商品实例；复制搭配组会复制组名、全部组内 SKU 和搭配关系。复制后的实例可继续拖动、重新组合或加入其他搭配组，同一个原始 SKU 可同时存在于多个搭配组中，各实例的拖动和移除互不影响。
- 搭配组内部按原始 SKU 去重，同一个 SKU 在一个搭配组内最多出现一次。将相同 SKU 的复制实例拖入已包含该 SKU 的搭配组，或将两个相同 SKU 实例互相拖动组成新搭配组时，系统不执行本次拖动并提示“同一搭配组内不能重复添加相同 SKU”；原实例位置和原有搭配关系保持不变。
- 右侧“本次制作”的“商品”有选择时按“n 商品 m SKU”展示，其中商品数按 SPU 去重统计，SKU 数按原始 SKU 去重统计，复制产生的商品实例不重复计数；商品数和 SKU 数均为 0 时显示“未选择”。“商品”下方新增“搭配组”：第二步显示“搭配中”，第三步及之后按实际搭配组数量显示“n 个”，复制产生的搭配组计入实际数量。第二步的商品明细按去重后的 SKU 平铺展示；第三步及之后改为按搭配组逐组展示，先展示搭配组名称及组内 SKU 数，再展示该组下的 SKU 图片、商品名称和“类目｜款色｜货号”，一个搭配组一个搭配组连续排列。未手动组合的单个 SKU 实例在进入第三步时自动成为一个独立搭配组，名称使用“货号_款色”；有关联商品时生成视频名称使用“搭配组名称_展示模版名称”，未关联商品时使用“模特名称_展示模版名称”，生成的视频默认关联该搭配组内全部商品。
- 桌面端商品选择区每行展示 5 个商品卡片，窄屏根据可用宽度自动减少列数。
- 商品卡片展示主图、名称、类目、可用款色、季节和商品标签。
- 类目与款色合并为普通文本“类目｜款色”，不使用标签样式。
- 已有伴播视频数量大于 0 时才展示数量；点击数量跳转“伴播视频库”并按商品 ID 搜索。数量为 0 时不展示该字段。
- 添加商品弹窗仍以 SPU 商品块组织信息，支持整件选择全部 SKU，也支持只选择其中一个或多个款色 SKU。
- 搭配组遵循搭配位规则：上衣（内搭）、上衣（外套）、下装、鞋子、袜子、帽子、手套和眼镜均为单占位，每个搭配组内同一搭配位最多放置一个 SKU。连衣裙同时占用上衣（内搭）和下装两个搭配位；套装同时占用上衣和下装两个搭配位，上衣部分由系统根据商品分析结果判断为上衣（内搭）或上衣（外套）。
- 搭配位规则同时适用于拖入已有搭配组和两个单品拖动组成新搭配组；如果目标组已有被占用的搭配位，页面提示具体搭配位已存在商品且本次拖动不执行，源商品及原有搭配关系保持不变。

#### 步骤三：选择数字模特

- 一次制作仅选择一个数字模特。
- 选择卡片展示标准半身白底图和名称；真人形象名称下展示体型，动画形象名称下展示角色特征。
- 系统自动使用该模特已解析的人脸、表情和身形信息。
- 当前步骤支持“创建数字模特”和“详情”。
- 系统预设数字模特仅支持查看和选择；用户创建的数字模特支持删除，不支持编辑。删除必须二次确认，已被本次制作选中的模特删除后清空当前选择。

#### 步骤四：使用背景

- 使用背景用于确定伴播视频在直播画面中的展示位置及输出背景类型；用户必须选择一个使用背景后才能制作视频。
- 用户可从已有使用背景中选择，或点击“创建背景”创建新的背景配置。
- 所有使用背景卡片均提供“详情”入口，点击打开右侧详情抽屉，抽屉内展示背景名称和背景分辨率，并支持“编辑”和“删除”：编辑沿用原配置并覆盖保存，不重复创建；删除需二次确认，删除当前正在使用的背景后清空当前选择。包括“时尚简约_右半屏”在内的系统案例也允许编辑和删除。数字模特、展示模版、使用背景详情抽屉的编辑和删除操作栏固定在抽屉底部，不随详情内容滚动。
- 数字模特、展示模版、使用背景三类详情抽屉宽度统一为原详情抽屉的 80%，其他详情抽屉宽度不变。
- 数字模特分为系统预设和用户创建两类：系统预设仅支持查看详情，不提供编辑和删除；用户创建的数字模特在详情抽屉底部提供编辑和删除，删除需二次确认。
- 从数字模特、展示模版、使用背景详情抽屉触发的编辑弹窗和删除确认弹窗必须显示在详情抽屉上层，不能被详情遮罩或侧边看板覆盖。
- 创建背景分两步：第一步填写背景名称并选填上传完整背景画面；第二步根据第一步已选展示模版的生成尺寸，在画布中框选伴播视频展示区域。
- 使用背景不再单独配置生成视频尺寸。展示区域继承展示模版的生成比例，可拖动和等比缩放，但不能改变比例。
- 未上传背景画面时，第二步使用灰色外部区域中的白色 9:16 默认画布；框选区域用于明确伴播视频在直播画面中的位置和尺寸。
- 未上传背景画面时，默认生成绿幕背景视频；使用背景卡片以灰色外围、白色画布和框选绿幕区域示意输出效果。
- 上传背景画面时，使用框选位置对应的背景画面作为视频背景。
- 第一步“背景画面”下方提供“生成滤镜调整”：可分别选填上传“商品图”和“环境实拍图”，两张都上传后自动生成滤镜调整（不提供独立按钮），按两张图片的差异调整背景滤镜，用于消除商品放入背景场景后的光线与色差影响，并展示生成的滤镜参数；删除任一图片后生成结果同步清除。两项上传均使用与创建展示模版参考图一致的图片卡片样式，已上传只展示图片、删除后回到上传提示。生成滤镜调整下方提示“若上传商品图和环境实拍图，将会根据图片差异调整背景滤镜，让生成的视频更贴合背景环境”。滤镜相关上传与生成结果随使用背景一起保存，重新编辑时回填。
- 使用背景卡片的信息块自上而下展示：背景名称、“分辨率 · 滤镜调整”；不存在滤镜调整时只展示分辨率，不展示“滤镜调整”。
- 使用背景详情抽屉固定标题为“背景详情”，不展示原“使用背景”标题。详情自上下依次为：背景画面（完整展示，不裁切、不限高）、背景名称（使用标题字体）、名称下方小字“分辨率 · 滤镜调整”（无滤镜调整时不展示该项）；若该背景存在滤镜调整，再向下展示“滤镜调整素材”（使用标题字体）及其商品图与环境实拍图。

#### 步骤五：制作与确认

- 制作流程底部操作栏在步骤一至步骤五均固定于流程区域最下方。第二至第四步在“上一步”右侧提供“开始新制作”；第五步制作过程中在“取消任务”右侧提供“开始新制作”，待确认结果页继续提供该按钮；第一步不展示。
- 第二至第四步点击“开始新制作”时弹窗提示“开始新制作将清空当前任务选择，确认开始新的制作？”，用户可选择“确认”或“取消”；确认后清空当前选择并回到第一步，取消后保持当前步骤及选择不变。
- 第五步点击“开始新制作”时弹窗提示“开始新制作后，当前任务将保留至<历史制作任务>中，确认开始新的制作？”，用户可选择“确认”或“取消”；确认后当前任务继续处理并保留在历史制作任务中，同时清空制作页并回到第一步，取消后继续留在当前任务。
- 第四步选择使用背景后，主按钮为“制作视频”。
- 点击“制作视频”进入第五步并立即展示整体生成进度，不再要求用户二次点击开始生成。
- Demo 中制作等待页至少停留 5 秒；等待页和确认页均可通过确认弹窗开始新的制作，无需等待当前任务结束。
- 第五步制作等待页主提示展示“正在创建伴播视频任务”，标题下不再展示额外灰色说明文字，进度条、当前阶段和视频完成数量继续保留。
- 进度条同时展示百分比和视频数量进度，例如“5 / 16”。
- 制作过程中原“上一步”改为“取消任务”，不可返回修改前序选择。
- 停止任务后立即展示已生成视频供用户确认，并汇总“已生成视频、未生成、生成失败”数量；数量为 0 的统计项不展示。
- 第五步结果区的“已生成视频 n”统计与“确认生成结果”位于同一行并靠右展示，不单独占用一行；生成结果卡片不展示款色或关联商品信息，未关联商品时也不展示“不关联商品”，仅保留视频名称、状态、质检结果和操作。
- 停止任务后只为已生成视频创建确认卡片，未生成和失败视频不进入待确认列表。
- 系统自动完成商品款色识别、动作匹配、视频生成和基础质检。
- 完成后按商品展示待确认结果。
- 支持预览、单条确认、重新生成和全部确认，不使用“审核”“通过”等流程化文案。
- 生成结果的预览弹窗底部提供“确认”按钮，确认后同步更新对应结果卡片；其他图片或视频预览不展示该按钮。
- 生成结果的预览弹窗采用左右布局：左侧预览视频，右侧展示“关联商品”商品图；搭配组视频展示组内全部关联商品图，相同 SKU 去重展示。
- 生成结果预览弹窗高度在上一版基础上再次增加 30%，目标高度约 676px，并受浏览器可视高度限制；视频区域与右侧关联商品区域随弹窗同步拉高，历史任务和伴播视频库的普通预览尺寸保持不变。
- 生成结果的预览弹窗同时提供“重新生成”和“废弃”：重新生成仅更新当前视频，废弃需二次确认，废弃后移出本次确认且不进入“伴播视频”。
- 当本次结果包含废弃视频时，处理完成页展示“本次结果已处理完成”，避免误写为“已全部确认”。
- 单条确认后，该结果立即从当前确认页移除并进入“伴播视频”列表；全部确认时批量移入伴播视频列表。
- 生成结果在桌面端每行展示 3 个卡片，窄屏自动降为单列。
- 全部视频确认完成后，第五步展示“已全部确认”和“继续制作”按钮，不自动跳转。
- 用户点击“继续制作”后，系统清空本次选择并返回第一步。
- 确认后的视频自动进入伴播视频库。

#### 历史制作任务

- 在制作伴播页面右侧“本次制作”上方展示“历史制作任务”入口。
- 入口展示“待确认 x”数量标签，实时反映待确认任务数；数量为 0 时隐藏标签。
- 点击进入生成任务列表，一个视频对应一条任务记录。
- 列表展示任务 ID、视频封面与视频 ID、名称、关联 SKU 白底图、状态、创建人、创建时间和操作。名称下方按“模特｜展示模版｜使用背景”的顺序展示纯文本值，不添加字段前缀。一个搭配组生成一条任务，任务关联搭配组内全部 SKU；列表只展示 SKU 图片，不直接展示商品名称、商品 ID、货号和款色。
- 鼠标移入任意关联 SKU 图片时，通过浮窗展示该 SKU 的商品名称、商品 ID、货号和款色；多商品搭配按 SKU 展示多张图片。
- 状态流转为：排队中 → 生成中 → 待确认 → 已完成 / 已废弃；排队中的任务也可取消并进入已取消。
- 排队中操作为“取消任务”，取消后尚未生成的视频停止制作，任务进入已取消；生成中无操作。
- 待确认操作为“去确认”，点击打开与制作确认一致的视频预览弹窗，支持“确认”“废弃”和“重新生成”。确认后任务进入已完成，视频进入伴播视频库；废弃后任务进入已废弃，视频不进入伴播视频库。
- 已完成和已废弃任务的预览弹窗同样支持“重新生成”。重新生成需二次确认，确认后新视频进入待确认状态，当前视频不再作为可用视频展示。
- 已完成操作为“查看”，打开视频预览弹窗；已完成视频支持改为废弃，改为废弃后从伴播视频库隐藏。
- 已废弃操作为“查看”，打开视频预览弹窗；已废弃视频支持“改为可用”，改为可用后任务进入已完成，视频重新进入伴播视频库。
- 已取消任务不生成视频，不提供视频预览和后续操作。
- 历史任务预览弹窗只展示视频和当前状态允许的底部操作，不重复展示任务状态、数字模特、商品和展示模版信息。
- 默认按待确认、生成中、排队中、已完成、已废弃、已取消排序，同状态按创建时间倒序。
- 支持按任务 ID、视频名称、商品 ID 搜索，并支持按状态和创建人筛选；搜索、状态筛选和创建人筛选保持在同一行。

### 5.2 伴播视频

- 视频类型仅包括“生成视频”和“上传视频”。
- 生成视频由商品搭配组、数字模特和展示模版生成；有关联商品时名称规则为“搭配组名称_展示模版名称”，未关联商品时名称规则为“模特名称_展示模版名称”。货号随抖店商品同步进入系统。
- 上传视频不单独填写名称，视频名称直接使用上传文件名。
- 上传视频分两步完成：第一步上传视频并解析视频长宽比；仅支持上传mp4格式视频，大小60M以内，支持绿幕视频。第二步在灰色外部区域中的白色 9:16 画布内设置展示位置，并支持从“定位参考图片”库选择图片。参考图片以图片卡片展示，不额外展示名称或说明文字；“新增上传”以左侧线框卡片展示，卡片内使用加号和“上传定位参考图”文字。参考图片支持新增上传和删除，上传后的图片保存到当前浏览器的演示数据中，刷新后仍可复用；选中的图片在白色画布中完整展示，伴播视频展示区域叠加在参考图片上，可移动、可等比缩放，但不能改变视频比例，便于视频背景与直播间背景精准嵌合。保存视频时记录所选参考图片。
- 上传视频的触发方式与展示模版、批量修改触发条件使用同一套定义：商品讲解、商品讲解 > 主播口令、商品讲解 > 弹幕评论、主播口令和弹幕评论。
- 商品讲解 > 主播口令、商品讲解 > 弹幕评论、主播口令和弹幕评论分别配置各自关键词；商品讲解不需要额外配置。
- V3 暂不支持活动节点触发，所有页面、筛选和插件统计中均不展示该选项。
- 支持按名称或关联商品搜索。
- 列表展示选择框、视频类型、视频封面、视频 ID、名称、关联 SKU 白底图、触发方式、播放次数、创建人和创建时间。名称下方按“模特｜展示模版｜使用背景”的顺序集中展示纯文本值，不添加字段前缀；上传视频仅展示已存在的信息。一个视频可关联多个商品 SKU，但仅可关联一个数字模特，上传视频可不关联数字模特，生成视频的数字模特取自制作时所选数字模特。
- 关联商品列只展示 SKU 图片，不直接展示商品名称、商品 ID、货号和款色。鼠标移入单张图片时，通过浮窗展示对应 SKU 的商品名称、商品 ID、货号和款色；多商品搭配视频展示全部关联 SKU 图片。
- 点击伴播视频库中的视频封面只打开播放预览弹窗，不展示废弃、重新生成、确认等操作；历史制作任务中的视频预览仍按任务状态展示对应操作。
- 支持类型、触发方式、创建人筛选。
- 支持批量修改触发条件、批量修改关联商品、批量修改关联模特、批量修改展示位置和批量废弃。默认不展示列表勾选框：用户先选择具体批量操作，再展示勾选框、全选、确认与取消，避免在选择前不清楚批量操作的目的。
- 批量修改触发条件弹窗支持多选触发方式，并按所选方式分别显示商品讲解口令、商品讲解弹幕、全局口令和全局弹幕关键词输入框。
- 批量修改关联商品支持按商品名称或商品 ID 搜索和多选；不选择商品保存时，视频改为不关联商品。
- 批量修改关联商品和批量修改关联模特仅适用于上传视频。进入这两种批量操作后，生成视频的勾选框置灰且不可选择；关联模特支持选择一个数字模特，或设为不关联数字模特。
- 批量修改展示位置仅适用于上传视频，且一次仅允许选择一条视频；全选不可用，生成视频勾选框置灰。弹窗不重复展示上传视频步骤，提供横向紧凑的“定位参考图”图库：可选择已有图片、新增上传或删除任意图片；删除操作优先于图片选中，避免点击删除时误选图片。选中图片后在白色位置画布中完整展示，用户在图片上调整伴播视频位置，保存时同步保存所选参考图片和位置参数。删除当前正在使用的参考图片时，当前编辑不再使用参考图，画布恢复为空白状态；图库删空后刷新页面也保持为空，不自动恢复已被删除的示例图。
- “修改展示位置”弹窗压缩定位参考图区与展示位置之间的空白，将主要空间用于 9:16 展示画布；视频尺寸比例和操作说明展示在“展示位置”标题右侧。X、Y 位置参数及方向微调统一放在画布左侧。“全屏放大”仅放大“展示位置”区域，不改变整个弹窗尺寸；放大后保留标题、比例说明、位置参数、微调控件和画布，并可退出全屏恢复原布局。
- 展示位置支持拖动、等比缩放及上下左右微调。弹窗左侧提供“视频大小缩放”和“左上角点位”参数：缩放值为展示框宽度相对画布宽度的百分比；左上角点位以 X、Y 两个输入框填写，表示展示框左上角相对画布左上角的位置。三项参数均保留两位小数，并与画布拖动、缩放、方向微调实时双向同步，便于多个视频直接复用相同位置。填写缩放时优先保持目标大小，必要时自动内收左上角点位，避免展示区域超出画布。四向按钮中间展示可编辑的微调参数，单位为百分比，默认值为 0.01%，支持用户在 0.01%～1% 之间修改；点击方向按钮或画布聚焦后使用键盘方向键，均按当前百分比移动。任何参数调整均不得使展示区域超出画布边界。
- 批量废弃不物理删除视频，仅将视频标记为已废弃，并从伴播视频库隐藏；关联历史制作任务同步进入已废弃状态。
- 伴播视频列表不设置单独操作列，不提供“播放”“查看审核”或“详情”文字操作。
- 点击任意视频封面直接弹窗播放视频；商品图片仍保持图片放大预览交互。
- 触发方式在最终视频层配置，避免在动作、动作流和视频中重复设置。

### 5.3 商品能力（集成于制作伴播第二步）

- 商品由授权店铺同步，也可保留人工导入能力。
- 支持按商品名称或商品 ID 搜索。
- 不再提供独立商品列表菜单。
- 制作伴播第二步展示当前店铺、切换店铺和添加商品操作。
- 商品卡展示商品主图、商品名称、商品 ID、二级类目、款色、季节、标签和已有伴播视频数量。
- 点击伴播视频数量，跳转“伴播视频”菜单并自动按当前商品 ID 搜索。
- 商品分析识别模特图和不同款色白底图；每张白底图必须标注对应款色。
- 类目、标签和抖店商品状态在表头筛选，标签和类目支持搜索选择。
- 商品卡支持多选后直接进入下一步，不再提供独立列表的批量操作。
- 颜色属于 SKU 信息，不作为普通商品标签重复展示。

### 5.4 数字模特能力（集成于制作伴播第三步）

- 数字模特分为“真人形象”和“动画形象”两类，但两类资产在同一页面混合展示，不使用 Tab 或分类页面。
- 卡片图片右上角标识形象类型；搜索和数量统计覆盖页面内全部数字模特。
- 创建数字模特时需要选择形象类型、模特名称、1 张正脸图片；真人形象在正脸图片上传前还需选择体型，体型包括标准、瘦小、高瘦、高壮和肥胖，参考图片为选填素材。
- 不再提供独立数字模特菜单；创建、选择和查看详情均在制作伴播第三步完成。
- 数字模特的可选范围由第一步所选展示模版类型决定：服饰展示模版只允许选择真人形象；IP形象互动模版只允许选择动画形象。不兼容的模特卡片置灰并说明原因（“当前模版类型不可用”），仅不可选择，仍支持点击“详情”查看模特详情。
- 数字模特卡片信息块：模特名称下方展示“模特类型 · 体型”（如“真人形象 · 瘦小”）；动画形象无体型，只展示“动画形象”。信息块不展示形象类型、参考图数量等信息表格。
- 系统根据上传素材自动生成抠图后的正面半身白底图，卡片左下角展示正面脸部白底图。
- 数字模特分析结果展示形象类型、体型和参考图数量，参考图区完整展示本次上传的全部参考图（含第一张），不再单独展示“正脸图”。
- 真人数字模特卡片在名称下展示所选体型；动画数字模特卡片在名称下展示角色特征。
- 数字模特资产包括人脸素材、表情素材、身形素材和身体结构信息。
- 创建弹窗支持“1 张正脸图片 + 多张参考图片 + 选填形象视频”。正脸图片用于锁定核心识别特征；参考图片最多 9 张，用于补充全身正面、侧面、背面、表情、典型动作和道具互动；形象视频用于补充连续动作、转身和动态形变。
- 真人形象重点解析人脸、表情、身形和身体比例；动画形象重点解析固定角色特征、画风、头身比例、服饰配色、纹理和动态特征。
- 生成后的展示：分析结果弹窗与数字模特详情抽屉共用同一套渲染。分析结果弹窗依次展示“分析生成图”、模特概览（名称、形象类型、体型、参考图数量）、参考图区（完整展示本次上传的全部参考图），底部操作为“废弃 / 重新生成 / 确认”。
- 数字模特详情抽屉固定标题为“模特详情”，不展示模特名称。详情自上下依次为：分析生成图（不展示“分析生成图”文本）、模特名称、名称下方小字“模特类型 · 体型”（动画形象无体型，只展示模特类型）、参考图区；不展示形象类型与参考图数量等信息表格。
- 系统预设的数字模特与用户创建的数字模特使用完全一致的详情结构，不因来源或形象类型区分展示样式。

### 5.5 展示模版能力（集成于制作伴播第一步）

- 不再提供独立展示模版菜单；创建、选择、查看详情、修改和删除均在制作伴播第一步完成。
- 展示模版只表示单个展示动作，不展示动作流。
- 展示模版分为“服饰展示”和“IP形象互动”两类。数据块展示封面、模版名称、模版类型、触发方式、使用次数、创建人和创建时间，不展示“展示搭配”或适用类目。
- 名称下的触发方式必须展示完整触发信息：无关键词时显示“商品讲解触发”；主播口令或弹幕评论配置了关键词时，显示为“主播口令“看一下模特效果”触发”或“弹幕评论“看一下蓝色款”触发”。多种触发方式用分号分隔。
- 展示模版详情抽屉固定标题为“模版详情”，不展示模版名称。详情自上下依次为：示例视频（视频封面 + 原生播放器，支持播放）、模版名称、名称下方小字“模版类型 · 创建信息”、生成参数（提示词、生成尺寸、生成时长）、触发方式（触发关键词直接合并展示在“触发方式”中）、参考素材。参考图、参考视频、参考音频作为独立区块统一放在最下方，且仅在实际存在时展示：参考图以缩略图列表展示并支持点击放大；参考视频和参考音频使用原生播放器，均支持在线播放和下载原文件，下载按钮放在“参考视频”“参考音频”标题的右侧。
- 展示模版卡片的信息块自上而下展示：模版名称、“尺寸 X · 时长 Y 秒”、触发方式。
- 创建展示模版生成前配置基础信息和生成方式。确认示例视频时即保存为可用模版，再进入触发方式设置；此时取消或关闭，模版仍保留为“触发方式未设置”，可用于制作视频，生成的视频继承“未设置”触发状态。保存触发方式时更新原模版，不重复创建。
- 服饰展示模版参考图结构为：参考图 1 为必填的“模特上身正面图”、参考图 2 为选填的“服饰搭配反面图”，两者均系统预置默认参考图（用户可直接套用，无需自行制作）；预置图支持删除后上传用户自己的图片。参考图 3 起为非指定选填参考图，不限定内容，支持新增、拖动排序和删除。图片卡片状态样式统一：已有内容（已上传）使用主色实线边框，空槽位使用虚线边框；鼠标移入空槽位时高亮为主色。示例视频生成时提示“请稍后，预计等待 3 分钟”。Demo 保持短时模拟，不实际等待数分钟。
- 创建展示模版创建流程扩展为“基础信息 → 生成方式 → 制作与确认 → 触发方式”。制作与确认阶段直接基于参考图生成模版示例视频（图生视频），不再包含先生成模特穿着效果图的图生图步骤；视频生成完成后确认即保存，原确认阶段的“取消”改为“废弃”。服饰展示模版是否标识“需要服饰反面图”取决于创建时是否提供了参考图 2“服饰搭配反面图”：提供了才标识，标签在卡片底部靠左展示、「详情」按钮在同行右侧；制作时上衣、下装搭配位仅允许选择带反面图的商品；未提供则不标识，制作时不对商品的反面图做限制。
- 基础信息必须配置“模版名称”“模版类型”和“创建方式”。模版类型可选“服饰展示”或“IP形象互动”；创建方式可选“已有模版修改”或“空白创建”。
- “创建方式”使用紧凑的双列单选项展示，单选控件固定为正圆形且不随卡片宽度拉伸，避免占用过多基础信息区域。选择“已有模版修改”时，基础信息下方提供可搜索的已有模版选择框，并仅展示当前模版类型下的已有模版；进入第二步后直接预填该模版的参考素材、提示词、生成尺寸和生成时长，用户可在此基础上修改。
- 创建展示模版弹窗的内容区和底部操作栏分离，内容区独立滚动，底部操作栏保持合适的固定高度；内容区末尾预留空隙，避免最后一个表单字段紧贴按钮。
- 选择“空白创建”时，进入第二步后不预填已有参数，用户从零配置生成方式。
- 生成方式不再在“参考图 + 提示词”和“参考视频”之间二选一，也不配置“展示搭配”。同一页面同时提供参考图、参考视频、参考音频和提示词，其中参考图与提示词为主要生成参数，参考视频、参考音频为选填。
- 参考图最多上传 9 张，每行最多展示 5 张。服饰展示模版固定预留参考图1模特图、参考图2服饰搭配正面图、参考图3服饰搭配反面图；IP形象互动模版固定预留参考图1 IP形象图。选填参考图不预留空位置，仅在用户上传后展示，并保留一个“添加参考图”入口，达到9张后隐藏添加入口。
- 固定必填参考图的位置不可拖动：服饰展示锁定参考图1-3，IP形象互动锁定参考图1。其他已上传的选填参考图支持鼠标拖动调整相互顺序；调整后的参考图顺序随模版生成参数保存并在再次打开时回填。
- 所有已上传参考图右上角展示删除叉号。删除固定必填参考图后保留对应必填空位；删除选填参考图后直接移除该图片卡片，并重新连续编号剩余选填参考图。
- 参考图下方统一提示：“请勿上传场景图，展示模版的生成示例统一使用室内白色场景”。
- 创建展示模版开始分析后，分析结果确认区域只展示生成的示例视频播放器，不展示封面旁的模版信息、触发方式或其他结果说明；确认、重新生成和修改生成参数等操作保持不变。
- 生成尺寸支持 1:1、4:3、3:4、16:9 和 9:16，不支持自动匹配和 21:9；默认选中 1:1；生成时长支持 1-15 秒。使用背景步骤继承该生成尺寸，仅配置伴播视频在直播画面中的展示位置。
- 触发方式包括商品讲解、商品讲解 > 主播口令、商品讲解 > 弹幕评论、主播口令和弹幕评论；涉及主播口令或弹幕评论时支持配置对应关键词。
- “商品讲解 > 主播口令”与“主播口令”分别配置关键词；“商品讲解 > 弹幕评论”与“弹幕评论”也分别配置关键词。前两类为关联商品的讲解期间定向触发，后两类为不依赖商品讲解的全局触发。
- 商品讲解相关触发方式必须关联商品。伴播视频库中，如未关联商品则在“关联商品”字段用红色文字提示“当前触发方式须关联商品”。
- 直播中控台中，弹幕评论触发的当前播放视频需展示实际命中的评论内容，例如“三杯鸡：看一下蓝色款”。
- 创建弹窗的步骤按钮随流程变化：第一步展示“下一步”，第二步展示“上一步、开始分析”；生成结果保留“取消、修改生成参数、重新生成、确认”。确认后隐藏步骤条及生成结果，仅展示触发方式表单和“取消、保存并完成”。五种触发方式保持多选，四种口令/评论触发分别填写关键词，保存时校验所选方式及其关键词。
- 进入下一步前按模版类型校验固定必填参考图和提示词；主播口令和弹幕评论触发必须填写对应关键词。静态 Demo 可自动补齐演示素材，不能因必填校验阻断演示流程。
- 点击“开始分析”后在当前弹窗展示分析进度，依次进行素材完整性检查、动作视频生成、动作姿态与镜头节奏识别和封面生成。
- 创建弹窗高度不超过当前浏览器视口，头部步骤和底部操作区固定，仅中间内容区滚动；复选框和单选框使用系统统一尺寸。
- 分析过程包括素材检查、动作视频生成、动作姿态与镜头节奏识别和封面提取；分析进行中不可误关闭弹窗。
- 分析完成后展示动作视频预览、封面和触发方式摘要，并支持返回修改。用户点击“确认创建”后，展示模版才进入模版列表。
- 展示模版生成结果的操作区除“取消”“重新生成”“确认”外，增加“修改生成参数”；该按钮位于“取消”右侧，点击后返回第二步，仅解锁生成方式参数进行修改，第一步和第三步配置保留，修改后重新开始分析。
- 制作伴播时由系统根据动作能力自动组织生成方案，用户不需要配置动作流。

### 5.6 账号与店铺

- 集中管理直播账号接入和抖店授权。
- 直播账号用于直播间连接和伴播控制。
- 店铺用于同步商品。
- 两类账号在同一页面分区展示，减少设置菜单层级，但各自字段保持完整。
- 直播账号展示头像、名称、抖音号、接入时间、接入人和移除操作。
- 店铺展示店铺头像、名称、店铺 ID、授权状态、授权到期时间和对应操作。

### 5.7 团队管理

- 角色层级：系统管理员 > 管理员 > 团队成员。
- 上级可管理下级，同级和下级不可管理上级。
- 系统中必须存在至少一个系统管理员。
- 支持新增成员、修改成员信息、关闭权限和恢复权限。
- 列表展示账号名称、手机号、权限角色、账号状态和操作。
- 所有会改变账号或权限的操作均需二次确认。

### 5.8 直播中控台浏览器插件

- 伴播控制顶部提供“数字模特”选择，默认选项展示为“全部”，也可指定一个数字模特；下方说明为“仅自动触发所选模特的视频和未关联模特的视频，其他模特视频需手动选择播放”。
- 指定数字模特后，商品讲解等自动触发从该模特及未关联数字模特的视频中选择；当前正在播放的视频不被强制中断。
- 手动“立即播放”和“下一条播放”不受数字模特筛选限制，允许人工选择其他模特的视频。
- 播放队列中，顶部展示当前播放视频及后续队列；当前播放视频和队列第一条待播放视频均为锁定状态，不允许被立即播放、移除或拖动调整；第二条及之后的视频支持正常修改队列顺序、立即播放和移除。播放队列区域不再展示进度条、“停止”和“播放下一个”操作。
- 当前播放、播放队列、商品视频浮窗和伴播视频列表均展示数字模特名称，帮助中控判断将要播放的形象。
- 插件内直播连接状态、数字模特说明、当前播放元信息和播放队列元信息统一使用 13px 辅助文字字号。
- “伴播视频”页在搜索框左侧提供独立的数字模特筛选，默认展示“全部”；该筛选只影响视频列表，不改变伴播控制页的自动触发数字模特设置。
- 插件端直播商品列表中，有伴播视频的商品统一展示“1 个伴播视频”，悬浮面板同步展示 1 条可手动播放的视频；无视频商品不展示入口。
- 伴播控制新增“商品讲解播放方式”，可选“循环播放”和“单次播放”。
- 循环播放：点击直播中控台“讲解商品”后，该商品所有符合数字模特筛选且触发方式为“商品讲解”的视频轮流循环播放，直至结束商品讲解或人工停止。
- 单次播放：点击“讲解商品”后，每条匹配视频仅播放一次，全部播放完后停止，不重新加入队列。
- 手动立即播放和下一条播放不受商品讲解播放方式限制。

## 6. 简化规则

1. 不删除对用户判断、筛选、追溯或操作有帮助的字段。
2. 同一信息不在菜单、列表和详情中重复说明。
3. 完整信息可以通过列表、详情和按需展开分层展示，但不能直接丢失。
4. 所有详情信息每行只展示一个字段，字段名固定宽度、字段值左对齐，长内容自然换行。
5. 系统状态转译为用户结果，例如“制作中”“待确认”，不展示内部子任务。
6. 批量操作只在用户勾选内容后出现。
7. 可直接播放、停止、调整队列的直播操作不增加二次确认。
8. 删除、权限变更、解除授权等不可逆或高风险操作必须二次确认。
9. 通用二次确认弹窗不展示顶部标题栏和右上角关闭按钮；弹窗宽度为 416px、整体最小高度为 282px，较上一版宽高均缩减约 20%。确认说明在正文区域水平、垂直居中；内容较多时弹窗可继续向下撑开，底部仅保留“取消”和“确认”操作。

## 7. 权限策略

- 团队成员：制作伴播、确认自己创建的视频、使用伴播视频和查看素材。
- 管理员：拥有团队成员能力，可维护展示模版并管理团队成员。
- 系统管理员：拥有全部能力，可管理管理员、账号接入和店铺授权。
- 数字模特、展示模版和使用背景统一区分“系统预设”与“用户创建”：系统预设内容仅支持查看详情和选择使用，不展示编辑、删除入口；用户创建的数字模特仅支持删除，用户创建的展示模版和使用背景支持编辑和删除。
- 三类资产详情均使用侧边详情面板，操作区固定在面板底部，不随详情内容滚动。系统预设内容的固定操作区展示“系统预设，仅支持使用”。
- 编辑展示模版使用单个编辑弹窗，不分步骤，仅展示并可修改“模版名称”和“触发方式”（含对应关键词）；模版类型、创建方式、生成方式、参考素材、提示词、生成尺寸和生成时长一律不展示、不允许修改，保存时沿用原配置。触发方式和对应关键词回填后可编辑。编辑使用背景时第一步和第二步均可编辑，可修改名称、背景画面和伴播展示区域。数字模特不提供编辑流程。
- 编辑保存后更新原资产，不新增重复记录；删除用户创建资产需二次确认。

不可操作的数据应直接禁用勾选和操作入口，不在用户点击后再提示无权限。

## 8. 原型范围

本版本为静态交互原型，覆盖：

- 后台信息架构和响应式布局
- 伴播制作五步流程
- 模拟生成进度和结果确认交互
- 制作流程内的商品同步与选择、数字模特管理、展示模版管理
- 历史制作任务和伴播视频库管理
- 账号店铺和团队管理弹窗
- 直播中控台浏览器插件、数字模特自动触发控制和商品讲解播放方式

暂不包含：

- 抖音开放平台真实授权和数据同步
- AI 视频生成接口
- 数据持久化
- 消息通知和计费结算

## 9. 验收标准

1. 新用户进入系统后，可在不理解动作流和生成任务的情况下完成一批伴播视频制作。
2. 主流程固定为五个步骤，每一步只有一个主要决策。
3. 左侧只展示工作台、制作伴播、伴播视频库、设置四个一级菜单。
4. 桌面端和移动端页面本身无横向溢出、文字遮挡和控件重叠；字段较多的表格在表格容器内横向滚动。
5. 生成结果必须经过人工确认后进入伴播视频库；全部确认后展示完成状态，点击“继续制作”才返回第一步。
6. 制作伴播各步骤、伴播视频库、设置页面具备本 PRD 要求的完整展示字段和管理操作。
- 展示模版、数字模特和使用背景统一排序：先展示最近 7 日创建的资源并按创建时间倒序（最新创建的排在最前面）；7 日之前创建的资源按当前客户近 7 日使用次数倒序，再按所有客户累计使用次数倒序。新建的展示模版、数字模特和使用背景一律以创建当下的时间记录创建时间，确保新建资源优先展示。数字模特在“制作伴播”选择步骤额外先展示当前展示模版可选的模特，置灰不可选的模特统一排在后面（排序结果与卡片置灰状态始终一致）；“数字模特”资源管理页不绑定当前模版，仅按上述统一规则排序。展示模版卡片中“需要服饰反面图”标签展示在卡片底部、「详情」按钮的左侧（靠左展示），卡片底部不额外留空。

### 商品分析状态与模版要求
- 添加商品弹窗不根据展示模版的反面图要求提前限制商品，所有店铺商品均可选择并导入。
- 每家店每天默认自动分析销量 Top20 商品。
- 自定义上传的商品同样需要经过 AI 分析：保存后进入“分析中”，加入制作时按分析的完成节奏出结果，再按展示模版要求判定是否符合；排序与分析完成后的处理与店铺商品完全一致。
- 已导入但尚未完成分析的商品，在商品搭配页面置灰，并在商品图片正中央叠加展示“AI分析中”状态标签（深灰色底、白色加粗字体，不展示在卡片文字区域下方）；不可拖动搭配、不可复制，只能移除。右侧“本次制作”商品列表不展示该商品。
- 选择商品页面展示排序规则：搭配组优先展示在最前，其次是符合当前展示模版要求的独立商品，再次是“AI分析中”的置灰商品，最后是“不符合要求”的置灰商品。
- AI 分析完成后，若符合当前展示模版要求，恢复正常展示，可进入右侧列表、拖动搭配、复制并进入下一步；若不符合要求，继续置灰，并在商品图片中央以同样式叠加展示具体原因，例如“缺少服饰反面图”，行为与“AI分析中”一致。已复用历史分析结果、但缺少当前模版所需视角图的商品，同样视为不符合要求并置灰标注原因。
- 点击下一步前自动移除所有“AI分析中”和“不符合要求”的商品，自动进入下一步，不阻断停留在商品选择步骤。
- 原型中模拟商品信息分析耗时约 10 秒：分析期间商品展示“AI分析中”，分析完成后按结果归类并重新排序。
